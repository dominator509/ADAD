# ASSUMPTIONS.md — ADAD

Assumptions made during blueprint generation. Each session that touches a
related area must verify the relevant assumption and update this file (confirm
or overturn) with a dated note.

| # | Assumption | Reason | Risk if wrong | How to verify | Blocks impl? |
|---|---|---|---|---|---|
| A1 | Base OS is Debian stable (bookworm or current stable) via `live-build`, hardened, not built from scratch. | User chose "hardened Debian Live base to build over." | Recipe/package names differ; hooks misfire. | Confirmed in EP-009/EP-010: the containerized builder uses Debian `trixie`, `live-build`, QEMU boot smoke, and the on-image leak battery. | No — EP-009 confirms. |
| A2 | The official MCP Rust SDK can provide ADAD's protocol layer while `agent-coding` owns execution, policy, and Claude-Code-like MCP ergonomics in-tree. | ADR-009 replaces the failed claw-code vendoring seam with an official protocol substrate and first-party execution logic. | The SDK could prove too limited for the desired parity surface, or static-musl constraints could bite later. | Verified in EP-002 on 2026-07-03: `agent-coding` now compiles and tests against `rmcp 2.1.0`, preserves first-party `mcp__server__tool` qualification, and passes `scripts/verify.sh`. | No — validated for the foundation seam; later plans expand parity. |
| A3 | `llama-server` (llama.cpp) exposes an OpenAI-compatible `/v1/chat/completions` endpoint. | Standard llama.cpp server behavior. | Client shape wrong; local path fails. | `llama-server --help`; hit the endpoint against a tiny GGUF in EP-004. | No. |
| A4 | Venice API is OpenAI-compatible at `https://api.venice.ai/api/v1` with Bearer auth and its own model IDs; "private" vs "anonymized" model classes exist. | Confirmed via Venice docs during design. | Fallback client or privacy default wrong. | Re-read Venice docs at implementation; mock in EP-004 tests. | No. |
| A5 | Target hosts are x86_64 with AVX; 16–64 GB RAM; USB 3.2 Gen2 or NVMe-in-USB. | Stated constraints. | Perf targets unmet on weaker HW. | EP-010 confirmed killswitch latency and mock VPS timing. Local inference tok/s remains unmeasured because no allowed benchmark command, pinned GGUF fixture, or throughput band exists; see `docs/EP-010-performance-review.md`. | No — residual launch risk documented. |
| A6 | Static musl builds are achievable for all core crates (no crate forces glibc/dynamic). | Constraint requires it. | A needed crate breaks static build. | `scripts/build.sh` passes in EP-010 verification. On Windows/Git-Bash it explicitly skips local musl execution; Linux builder/CI remains authoritative for execution. | No — verified within documented host boundary. |
| A7 | Monero access is via remote nodes through `monero-wallet-rpc` (no local full node). | Stated non-goal: no local node. | Wallet ops fail if RPC shape assumed wrong. | Confirm `monero-wallet-rpc` flags/JSON-RPC in EP-004 with a mock. | No. |
| A8 | VPS providers (1984.is/Njalla/BuyVM) accept SSH provisioning over Tor and XMR payment out-of-band. | Stated integrations. | Provider flow differs; automation stalls. | EP-004 uses a mock SSH target; real provisioning is a STOP (spends money). | No. |
| A9 | Git identity requirement = single stable pseudonym + stripped real metadata (NOT per-push rotation), and MAC handling = randomization (NOT impersonation). | User reframed goal to leak-prevention, not evasion. | Building rotation/impersonation would be out of scope and wrong. | SPEC-005; EP-006 asserts stable identity + randomized MAC. | No — settled. |
| A10 | No traditional DB; persistence is the LUKS2 vault + blockchain state. No schema migrations. | Stated stack. | Someone adds a DB/migration by habit. | ARCHITECTURE.md forbids it; review checklist. | No. |
| A11 | Tests never touch real devices/remotes/wallets; QEMU + loopback images + mocks only. | Safety + reproducibility. | A test could wipe a real disk or spend XMR. | Verified across EP-003 and EP-010: vault tests target image files, QEMU drills boot image artifacts only, rollback drill copies `build/adad.img` inside the builder container, and real device/VPS/XMR actions remain STOP-gated. | No — enforced. |
